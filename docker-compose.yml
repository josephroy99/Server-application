services:
        telescope-cad:
                build: ./telescopeCAD
                ports:
                        - "80:80"
        nginx:
                build: ./nginx
                ports:
                        - "8080:80"