services:
        telescope-cad:
                build: ./telescopeCAD
                ports:
                        - "80:80"
        nginx:
                build: ./vite-project
                ports:
                        - "8080:80"